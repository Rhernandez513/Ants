#ifndef CONTAINERS_H
#define CONTAINERS_H

namespace Ants {
struct Position {
  int x;
  int y;
};
enum Direction { LEFT, RIGHT, UP, DOWN };
enum Color { red, blue };
// Extensible Storage
}
#endif // CONTAINERS_H
